import { SxProps } from '@mui/material';

export const LoginContainerStyle: SxProps = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '100%',
};

export const CardContentStyle: SxProps = {
  maxWidth: 700,
};
